import routerConst from 'router/routerConst';

const routers = {
  dashboard: {
    exact: true,
    path: routerConst.DASH_BOARD,
    // component: Dashboard,
    // route: publicRoutes,
  },
};

export default routers;
